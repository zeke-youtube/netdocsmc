import Link from "next/link";
import { getCategories } from "@/lib/docs";
export function Sidebar({ currentSlug }: { currentSlug?: string }) { return <nav aria-label="API reference" className="w-full md:w-64 md:border-r md:border-slate-200 md:pr-6 dark:md:border-slate-800">{getCategories().map((category) => <section key={category.name} className="mb-5"><h2 className="mb-1 text-xs font-semibold uppercase tracking-wide text-slate-500">{category.name}</h2><ul>{category.entries.map((entry) => <li key={entry.id}><Link aria-current={entry.slug === currentSlug ? "page" : undefined} className={`block border-l-2 px-3 py-1.5 text-sm no-underline ${entry.slug === currentSlug ? "border-accent-600 bg-accent-50 font-medium text-accent-700 dark:bg-slate-900 dark:text-blue-300" : "border-transparent hover:bg-slate-50 dark:hover:bg-slate-900"}`} href={`/api/${entry.slug}`}>{entry.name}</Link></li>)}</ul></section>)}</nav>; }
