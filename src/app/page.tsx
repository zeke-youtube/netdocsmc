import Link from "next/link";
import { apiEntries, getCategories } from "@/lib/docs";
import { SearchBox } from "@/features/search/search-box";
export default function Home() { return <main className="mx-auto max-w-5xl px-4 py-14"><h1 className="text-4xl font-bold tracking-tight">NetDocs MC</h1><p className="mt-3 max-w-2xl text-lg text-slate-600 dark:text-slate-300">A searchable, community-maintained reference for the NetEase Minecraft China Edition ModAPI. Original Chinese documentation is preserved alongside translations when available.</p><div className="mt-8"><SearchBox entries={apiEntries} /></div><section className="mt-12"><h2 className="text-xl font-semibold">Browse by category</h2><div className="mt-4 grid gap-4 sm:grid-cols-2">{getCategories().map((category) => <div key={category.name} className="border-t-2 border-slate-300 py-3 dark:border-slate-700"><h3 className="font-semibold">{category.name}</h3><p className="text-sm text-slate-500">{category.entries.length} API {category.entries.length === 1 ? "entry" : "entries"}</p><Link href={`/api/${category.entries[0].slug}`} className="mt-2 inline-block text-sm text-accent-700 dark:text-blue-300">Open reference</Link></div>)}</div></section></main>; }
